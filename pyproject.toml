[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "log-trigger"
version = "0.1.0"
dependencies = [
    "click>=8.0",
    "pyyaml>=6.0",
]

[project.scripts]
log-trigger = "test1212.cli:main"
